Before making a commit, be sure to run `bun fmt` and `bun lint:fix`.
Before making a commit, be sure to run `bun test`.
Before making a commit, be sure to run `bun typecheck`.
Ensure all commit message are detailed, with bodies containing relevant. information about the commit and in conventional commit format.
